# Arquitetura do Sistema - WhatsUpLeads

## 📐 Visão Geral

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │   /admin/*   │  │ /dashboard/* │  │    /api/*    │           │
│  │ (Super Admin)│  │   (Tenant)   │  │ (API Routes) │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
│         │                 │                 │                    │
│         └────────────┬────┴─────────────────┘                    │
│                      ▼                                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              /admin/workspace/* (Compartilhado)           │   │
│  │    Campanhas, Instâncias, Leads, Credenciais, etc.        │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                       BACKEND (API)                              │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐     │
│  │   Auth Layer   │  │  Workspace Auth │  │   API Auth     │     │
│  │  (JWT Cookie)  │  │ (Company Check) │  │  (API Key)     │     │
│  └────────────────┘  └────────────────┘  └────────────────┘     │
│                               │                                  │
│                               ▼                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    Prisma ORM                             │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                       DATABASE                                   │
│                      PostgreSQL                                  │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐         │
│  │Company│ │ User │ │Instance│ │ Lead │ │Campaign│ │Message│     │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘         │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                   PROVEDORES EXTERNOS                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │Meta Cloud│  │  WUZAPI  │  │  UAZAPI  │  │   PAPI   │         │
│  │(Oficial) │  │(Self-host)│  │(Self-host)│  │(Self-host)│       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Modelo de Dados

### Entidades Principais

```prisma
// Empresa (Tenant)
model Company {
  id          String     @id @default(uuid())
  name        String
  slug        String     @unique
  isMaster    Boolean    @default(false)  // Super admin company
  planId      String?
  users       User[]
  instances   Instance[]
  leads       Lead[]
  campaigns   Campaign[]
  credentials Credential[]
}

// Usuário
model User {
  id            String   @id @default(uuid())
  email         String   @unique
  password      String
  name          String?
  companyId     String
  isMaster      Boolean  @default(false)  // Super admin user
  mustChangePassword Boolean @default(true)
  company       Company  @relation(...)
}

// Instância WhatsApp
model Instance {
  id           String   @id @default(uuid())
  name         String
  provider     Provider // META_CLOUD, WUZAPI, UAZAPI, PAPI
  status       Status   // CONNECTED, DISCONNECTED, etc.
  companyId    String
  providerData Json     // Configurações específicas do provider
}

// Campanha
model Campaign {
  id          String   @id @default(uuid())
  name        String
  status      Status   // DRAFT, RUNNING, PAUSED, COMPLETED
  companyId   String
  rounds      CampaignRound[]
  instances   CampaignInstance[]
}
```

### Relacionamentos Multi-tenant

```
Company (1) ─────────────────── (*) User
   │
   ├──────────────────────────── (*) Instance
   │
   ├──────────────────────────── (*) Lead
   │                                  │
   │                                  └── (*) LeadListItem
   │                                           │
   │                                           └── LeadList
   │
   ├──────────────────────────── (*) Campaign
   │                                  │
   │                                  ├── (*) CampaignRound
   │                                  │        │
   │                                  │        └── (*) CampaignLead
   │                                  │
   │                                  └── (*) CampaignInstance
   │
   └──────────────────────────── (*) Credential
```

---

## 🔐 Fluxo de Autenticação

### Login Flow

```
┌─────────┐     ┌─────────────┐     ┌─────────────┐
│  User   │────▶│ /api/auth/  │────▶│   Prisma    │
│         │     │   login     │     │ (verify pwd)│
└─────────┘     └─────────────┘     └─────────────┘
                       │
                       ▼
              ┌─────────────────┐
              │ Generate JWT    │
              │ Set httpOnly    │
              │ Cookie          │
              └─────────────────┘
                       │
                       ▼
              ┌─────────────────┐
              │ Return session: │
              │ - userId        │
              │ - companyId     │
              │ - isMaster      │
              └─────────────────┘
```

### Request Authentication

```
┌─────────┐     ┌─────────────┐     ┌─────────────┐
│ Request │────▶│ Middleware  │────▶│ Check JWT   │
│ + Cookie│     │             │     │ Cookie      │
└─────────┘     └─────────────┘     └─────────────┘
                                           │
                       ┌───────────────────┴───────────────────┐
                       ▼                                       ▼
              ┌─────────────────┐                     ┌─────────────────┐
              │ Valid Session   │                     │ Invalid/Missing │
              │ Add x-pathname  │                     │ Redirect /login │
              └─────────────────┘                     └─────────────────┘
```

---

## 🎯 Fluxo de Workspace

### Acesso Super Admin

```
Super Admin ──▶ /admin/workspace/* ──▶ AdminLayout
                                            │
                                            ▼
                                    ┌─────────────────┐
                                    │ AdminSidebar    │
                                    │ (Full menu)     │
                                    └─────────────────┘
```

### Acesso Tenant

```
Tenant ──▶ /admin/workspace/* ──▶ WorkspaceLayout
                                       │
                                       ▼
                               ┌─────────────────┐
                               │ TenantSidebar   │
                               │ (Workspace menu)│
                               └─────────────────┘
```

### Detecção de Tipo de Usuário

```typescript
// app/admin/workspace/layout.tsx
export default async function WorkspaceLayout({ children }) {
  const session = await getSession();
  
  if (session?.isMaster) {
    return (
      <div className="bg-slate-900">
        <AdminSidebar />
        <main className="ml-64">{children}</main>
      </div>
    );
  }
  
  return (
    <div className="bg-slate-900">
      <Sidebar />  {/* Tenant sidebar */}
      <main className="ml-64">{children}</main>
    </div>
  );
}
```

---

## 📡 Provedores WhatsApp

### Abstração de Providers

```typescript
// lib/providers/index.ts
interface WhatsAppProvider {
  sendMessage(params: SendParams): Promise<SendResult>;
  getStatus(params: StatusParams): Promise<StatusResult>;
  connect(params: ConnectParams): Promise<ConnectResult>;
  disconnect(params: DisconnectParams): Promise<void>;
}

// Cada provider implementa a interface
class MetaCloudProvider implements WhatsAppProvider { ... }
class WuzapiProvider implements WhatsAppProvider { ... }
class UazapiProvider implements WhatsAppProvider { ... }
class PapiProvider implements WhatsAppProvider { ... }
```

### Seleção de Provider

```typescript
function getProvider(instance: Instance): WhatsAppProvider {
  switch (instance.provider) {
    case "META_CLOUD":
      return new MetaCloudProvider();
    case "WUZAPI":
      return new WuzapiProvider();
    case "UAZAPI":
      return new UazapiProvider();
    case "PAPI":
      return new PapiProvider();
    default:
      throw new Error(`Provider não suportado: ${instance.provider}`);
  }
}
```

---

## 💰 Sistema de Custos (Meta Cloud)

### Fluxo de Cálculo

```
┌─────────────┐     ┌─────────────────┐     ┌─────────────────┐
│ Campaign    │────▶│ Get Meta Prices │────▶│ Calculate Cost  │
│ Start       │     │ from Database   │     │ (leads × price) │
└─────────────┘     └─────────────────┘     └─────────────────┘
                                                    │
                                                    ▼
                                           ┌─────────────────┐
                                           │ Save Estimated  │
                                           │ Cost to Round   │
                                           └─────────────────┘
                                                    │
                    After 24h                       ▼
                    ┌─────────────────┐     ┌─────────────────┐
                    │ Fetch Real Cost │◀────│ Campaign        │
                    │ from Meta API   │     │ Completed       │
                    └─────────────────┘     └─────────────────┘
```

### Armazenamento de Preços

```
MetaPricing Table
┌──────────────────┬────────────────┬─────────────────┐
│ marketingUsd     │ utilityUsd     │ authenticationUsd│
│ (5 decimals)     │ (5 decimals)   │ (5 decimals)    │
├──────────────────┼────────────────┼─────────────────┤
│ 680 = $0.00680   │ 380 = $0.00380 │ 315 = $0.00315  │
└──────────────────┴────────────────┴─────────────────┘
         │                 │                 │
         ▼                 ▼                 ▼
┌──────────────────┬────────────────┬─────────────────┐
│ marketingBrl     │ utilityBrl     │ authenticationBrl│
│ (centavos)       │ (centavos)     │ (centavos)      │
├──────────────────┼────────────────┼─────────────────┤
│ 34 = R$ 0.34     │ 4 = R$ 0.04    │ 4 = R$ 0.04     │
└──────────────────┴────────────────┴─────────────────┘
```

---

## 📂 Estrutura de Diretórios

```
WhatsUpLeads/
├── app/
│   ├── admin/                    # Super Admin pages
│   │   ├── layout.tsx            # Admin layout (sidebar + auth)
│   │   ├── page.tsx              # Dashboard admin
│   │   ├── workspace/            # Área compartilhada
│   │   │   ├── layout.tsx        # Detecta tipo de usuário
│   │   │   ├── campaigns/
│   │   │   ├── instances/
│   │   │   ├── leads/
│   │   │   └── credentials/
│   │   └── [exclusivas]/         # companies, plans, billing, etc.
│   │
│   ├── dashboard/                # Tenant pages
│   │   ├── layout.tsx            # Dashboard layout
│   │   └── [exclusivas]/         # billing, plans, settings, etc.
│   │
│   └── api/
│       ├── admin/
│       │   └── workspace/        # APIs compartilhadas
│       ├── dashboard/            # APIs tenant
│       └── v1/                   # API pública
│
├── components/
│   ├── admin/                    # Componentes admin
│   ├── layout/                   # Sidebar tenant, etc.
│   └── ui/                       # Componentes reutilizáveis
│
├── lib/
│   ├── auth.ts                   # Funções de autenticação
│   ├── workspace-auth.ts         # Helper para workspace
│   ├── prisma.ts                 # Cliente Prisma
│   ├── providers/                # Provedores WhatsApp
│   ├── meta-pricing.ts           # Preços Meta
│   └── meta-cost-tracker.ts      # Cálculo de custos
│
└── prisma/
    ├── schema.prisma             # Schema do banco
    └── migrations/               # Migrations
```

---

## 🔄 Fluxo de uma Campanha

```
┌─────────────┐
│  1. CREATE  │  Cria campanha com leads e instâncias
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  2. DRAFT   │  Campanha aguardando configuração
└──────┬──────┘
       │
       ▼
┌─────────────┐     ┌─────────────────────────────────────┐
│  3. START   │────▶│ - Cria CampaignRound                │
└──────┬──────┘     │ - Snapshot conversas Meta (se Meta) │
       │            │ - Calcula custo estimado            │
       ▼            │ - Agenda mensagens na fila          │
┌─────────────┐     └─────────────────────────────────────┘
│  4. RUNNING │
└──────┬──────┘
       │
       ├──────────────────┬──────────────────┐
       ▼                  ▼                  ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  5. PAUSE   │    │  5. STOP    │    │ 5. COMPLETE │
│ (temporário)│    │ (permanente)│    │  (sucesso)  │
└─────────────┘    └─────────────┘    └──────┬──────┘
                                             │
                                             ▼
                                    ┌─────────────────┐
                                    │ 6. COST CALC    │
                                    │ (após 24h)      │
                                    └─────────────────┘
```

---

## 📝 Notas Importantes

1. **Multi-tenancy**: Sempre filtre por `companyId` em queries
2. **Preços**: Sempre em centavos, nunca com decimais
3. **Providers**: Use abstração, nunca chame APIs direto
4. **Theme**: Sempre dark, nunca condicionalmente
5. **TypeScript**: Use `!` para non-null quando garantido pela lógica
